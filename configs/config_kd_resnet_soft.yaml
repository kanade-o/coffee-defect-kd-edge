# configs/config_distill.yaml
gpu: 9
num_classes: 2

data:
  input_size: 224
  train_dir: /home/sota/research/sotaohnuma/coffee/data/bean_224/train
  val_dir: /home/sota/research/sotaohnuma/coffee/data/bean_224/val
  test_dir: /home/sota/research/sotaohnuma/coffee//data/bean_224/test

model:  # student の定義（従来どおり instantiate を使う）
  name: cnn_student100
  #_target_: model.mobilenetv2_050_np.mobilenetv2_050_np
  _target_: models.cnn_student100.cnn_student100
  batch_size: 128
  lr: 0.0020
  weight_decay: 0.0090
  num_classes: ${num_classes}
  student_weights_path: # /home/sota/research/sotaohnuma/coffee/src/models/weights/cnn_student.pt

teacher:
  weights_path: /home/sota/research/sotaohnuma/coffee/src/models/weights/resnet50_finetuned.pt
  num_classes: ${num_classes}     # 学習時のクラス数に合わせて

kd:
  T: 2.0          # ロジットKDの温度
  alpha: 0.5      # ロジットKDの係数

train:
  epochs: 50

hydra:
  run:
    dir: outputs/kd/cnn_student100/${now:%Y-%m-%d_%H-%M-%S}
  job:
    chdir: true
