\chapter{実験}\label{experiment}
本章では, 提案手法の有効性を検証するための実験設定について述べる.
なお, 使用するデータセットの詳細については前章で説明したとおりであり, 実験結果および考察は次章にて報告する.

\section{実験環境}
本実験では, 第~\ref{dataset}章で構築したコーヒー生豆欠点豆検出データセットを使用した.
教師モデルの学習には, NVIDIA TITAN X GPUをUbuntu 20.04上で使用した.
エッジデバイスでの推論評価には, 16GBのメモリを搭載したRaspberry Pi 5を使用し, Raspberry Pi OS (64-bit)上で動作させた.

\section{評価指標}
本研究では, モデルの性能を多角的に評価するため, 分類性能指標とエッジデバイス上での実行性能指標の両面から評価を行う.

\subsection{分類性能指標}
分類性能の評価には, 正解率(Accuracy), 適合率(Precision), 再現率(Recall), およびF1スコア(F1 score)を用いる.
これらの指標は, 混同行列の要素である真陽性(TP: True Positive), 真陰性(TN: True Negative), 偽陽性(FP: False Positive), 偽陰性(FN: False Negative)に基づいて算出される.

正解率は, 全サンプル中で正しく分類されたサンプルの割合を示す指標であり, 以下の式で定義される.
\begin{equation}
\textit{Accuracy} = \frac{TP + TN}{TP + TN + FP + FN}
\end{equation}
正解率はモデルの全体的な性能を把握するのに有効であるが, クラス間のデータ数に偏りがある場合には適切な評価指標とならない場合がある.
適合率は, モデルが正(欠点豆)と予測したサンプルのうち実際に正であったサンプルの割合を表し, 以下の式で定義される.
\begin{equation}
\textit{Precision} = \frac{TP}{TP + FP}
\end{equation}
適合率は誤検出の少なさを評価する指標であり, 値が高いほど偽陽性が少ないことを示す.

再現率は, 実際に正(欠点豆)であるサンプルのうちモデルが正しく検出できた割合を示し, 以下の式で定義される.
\begin{equation}
\textit{Recall} = \frac{TP}{TP + FN}
\end{equation}
再現率は見逃しの少なさを評価する指標であり, 値が高いほど偽陰性が少ないことを示す.
本研究では, 欠点豆の見逃しが品質管理上重大な問題となるため, 再現率を特に重視する.

F1スコアは, 適合率と再現率の調和平均として定義され, 以下の式で算出される.
\begin{equation}
\textit{F1 score} = 2 \times \frac{\textit{Precision} \times \textit{Recall}}{\textit{Precision} + \textit{Recall}}
\end{equation}
F1スコアは適合率と再現率の両者のバランスを考慮した総合的な分類性能を評価できる指標であり, どちらか一方の指標が極端に低い場合にはF1スコアも低くなる特性を持つ.

\subsection{エッジデバイス性能指標}
エッジデバイス上での実用性を評価するため, 平均レイテンシ(Average Latency), CPU使用率(CPU Usage), およびメモリ使用量(Memory Usage)を測定する.

平均レイテンシは, 1枚の画像に対する推論処理に要する平均時間をミリ秒(ms)単位で表した指標である.
本指標は, 100回の連続推論を実行し, その平均値として算出する.
レイテンシが低いほど高速な推論が可能であり, リアルタイム処理の実現可能性を評価する上で最も重要な指標となる.
また, 平均レイテンシの逆数からスループット(FPS: frames per second)を算出することで, 単位時間あたりの処理可能枚数を評価できる.

CPU使用率は, 推論処理中のCPUリソースの占有率をパーセンテージで示す指標である.
本指標は, psutilライブラリを用いて推論実行中のプロセスのCPU使用率を継続的に監視し, その平均値として算出する.
CPU使用率が高い場合, システム全体への負荷が大きく, 他の処理との並行実行が困難となる可能性がある.
エッジデバイスでは複数のアプリケーションが同時に動作することも想定されるため, 低いCPU使用率が望ましい.

メモリ使用量は, 推論プロセスが使用するメモリ量をMB単位で表す指標である.
本指標は, psutilライブラリを用いてプロセスの常駐セットサイズ(RSS: Resident Set Size)を測定し, MBに換算して算出する.
Raspberry Pi 5のような限られたメモリ容量を持つエッジデバイスにおいては, メモリ使用量が実装可能性を左右する重要な制約条件となる.
低いメモリ使用量は, 他のシステムプロセスやアプリケーションとの共存を可能にし, システム全体の安定性を向上させる.

\section{教師モデルの比較実験}
教師モデルの学習では, 各モデルの性能を最大限に引き出すため, 2段階の最適化手順を採用した.
まず, 各モデルについて利用可能なGPUメモリ内で実行可能な最大のバッチサイズを探索し設定した.
次に, 自動最適化フレームワークOptunaを用いて, 学習率($1 \times 10^{-5}$から$1 \times 10^{-2}$の範囲)および重み減衰($1 \times 10^{-5}$から$1 \times 10^{-1}$の範囲)について対数スケールでの探索を行い, 効果的な組み合わせを決定した.

学習率スケジューラには, 線形ウォームアップとコサインアニーリングを組み合わせた手法を採用した.
総学習ステップ数の最初の10\%をウォームアップ期間とし, その後コサインスケジュールに従って学習率を減衰させた.
学習エポック数は150に設定した.
最適化手法には全モデルでAdamWを使用した.

各モデルについて決定した最終的なハイパーパラメータを表\ref{table:hyperparameters}に示す.

\begin{table}[htbp]
\caption{各モデルのハイパーパラメータ設定}
\label{table:hyperparameters}
\begin{center}
\begin{tabular}{lccccc} \hline
  モデル & バッチサイズ & 学習率 & 重み減衰 & 最適化手法 & スケジューラ \\ \hline
  教師 (ResNet-50) & 137 & 7.00e-4 & 5.30e-5 & AdamW & Cosine (Warmup10\%) \\
  教師 (Swin-Small) & 66 & 1.37e-5 & 0.0081 & AdamW & Cosine (Warmup10\%) \\
  生徒 & 128 & 0.01 & 0.01 & AdamW & Cosine (Warmup10\%) \\ \hline
\end{tabular}
\end{center}
\end{table}

表~\ref{table:result_teacher_selection}に, 各教師モデルの分類性能評価結果を示す. 
本研究では, 欠点豆の見逃しを最小限に抑えることが重要であるため, 再現率を最も重視し, その次にF1スコアを重視して教師モデルを選定した. 
表~\ref{table:result_teacher_selection}を見ると, CNNベースのモデルでは, EfficientNet-B0が最も高い再現率(0.992)を達成したが, F1スコアは0.958であった. 
それに対し, 2番目に高い再現率(0.990)を示したResNet-50は, F1スコアも0.981と高く, バランスの取れた性能を示した. 
そのため, CNNベースの教師モデルとしてResNet-50を選定した. 
一方, Transformerベースのモデルでは, Swin Transformer Smallが最も高い再現率(0.994)とF1スコア(0.982)を達成した. 
したがって, Transformerベースの教師モデルとしてSwin Transformer Smallを選定した.

\begin{table}[htbp]
\caption{教師モデルの分類性能評価結果}
\label{table:result_teacher_selection}
\begin{center}
\begin{tabular}{llcccc} \hline
アーキテクチャ & モデル & 正解率 (\%)& 適合率 (\%)& 再現率 (\%)& F1スコア (\%) \\ \hline
\multirow{4}{*}{CNN} & CNN (自作) & 91.8 & 91.2 & 92.6 & 91.9 \\
 & EfficientNet-b0 & 95.6 & 92.5 & 99.2 & 95.8 \\
 & ResNet-18 & 97.5 & 98.1 & 96.8 & 97.5 \\
 & ResNet-50 & 98.1 & 97.3 & 99.0 & 98.1 \\\hline
\multirow{7}{*}{ViT} & ViT-b16 & 95.9 & 95.4 & 96.4 & 95.9 \\
 & DeiT-Small & 96.8 & 97.3 & 96.3 & 96.8 \\
 & DeiT-Base & 97.6 & 96.5 & 98.8 & 97.6 \\
 & DeiT-Base-Distilled & 97.6 & 96.5 & 98.5 & 97.5 \\
 & Swin-Tiny & 98.1 & 98.3 & 97.9 & 98.1 \\
 & Swin-Small & 98.2 & 97.0 & 99.4 & 98.2 \\
 & Swin-Base & 98.0 & 97.6 & 98.4 & 98.0 \\\hline
\end{tabular}
\end{center}
\end{table}


\section{知識蒸留の効果検証}\label{result_kd_effect}
生徒モデルの知識蒸留では, 教師モデルと同様の学習率スケジューラ(ウォームアップ+コサインアニーリング)を使用し, 150エポックの学習を行った.
蒸留時には, 蒸留損失項の重み$\alpha$を0.5, 温度パラメータ$T$を2.0に設定した.

知識蒸留の効果を検証するため, 生徒モデル単独での学習結果と, 各教師モデルから蒸留した場合の性能を比較した.
これにより, CNNベース教師モデルおよびTransformerベース教師モデルからの知識蒸留の有効性を評価した.

表~\ref{table:student_results}に, 知識蒸留の効果検証結果を示す. 
生徒モデル単独での学習では, 再現率91.0\%, F1スコア92.7\%を達成した. 
ResNet-50から蒸留を行ったモデルでは, 再現率88.5\%, F1スコア90.5\%と, 生徒単独学習を下回る結果となった. 
一方, Swin Transformer Smallから蒸留を行ったモデルでは, 再現率94.3\%, F1スコア94.5\%を達成し, 生徒単独学習を大幅に上回る結果となった. 
そのため, Swin Transformer Smallから知識蒸留を行った生徒モデルを最終的な提案モデルとして採用した.

\begin{table}[htbp]
\caption{生徒モデルの分類性能比較}
\label{table:student_results}
\begin{center}
\begin{tabular}{lcccc} \hline
モデル & 正解率 (\%) & 適合率 (\%) & 再現率 (\%) & F1スコア (\%) \\ \hline
生徒(蒸留なし) & 92.8 & 94.4 & 91.0 & 92.7 \\
生徒(ResNet-50からの蒸留) & 90.7 & 92.6 & 88.5 & 90.5 \\
生徒(Swin-Smallからの蒸留) & 94.5 & 94.8 & 94.3 & 94.5 \\\hline
\end{tabular}
\end{center}
\end{table}


\section{エッジデバイスでの性能評価}\label{edge_performance}
学習済みモデルの実用性を評価するため, Raspberry Pi 5上で推論ベンチマークを実施した.
測定にはPython標準ライブラリおよびpsutilパッケージを使用した.

まず, キャッシュおよびランタイム条件を安定させるため, 測定開始前に20回のダミー推論を実行した.
その後, 前述の評価指標に基づき, 推論時間とリソース使用量を測定した.
これにより, エッジデバイス上でのリアルタイム処理の実現可能性を評価した.

表~\ref{table:edge_performance}に, エッジデバイス上での推論性能評価結果を示す. 
提案モデルは, 平均レイテンシ9.05ms, スループット110.5FPSを達成し, 非常に高速な推論性能を示した. 
一方, 教師モデルであるResNet-50およびSwin Transformer Smallは, それぞれ364.7msおよび226.6msの平均レイテンシを示し, 
スループットでは2.7FPSおよび4.7FPSにとどまった. 
CPU使用率に関しては, いずれのモデルも96\%を超える高い値を示したが, 提案モデルは96.5\%と若干低く抑えられた.
メモリ使用量では, 提案モデルが3.16MBと非常に低く, ResNet-50の98.5MBおよびSwin Transformer Smallの185.3MBと比較して大幅に軽量であることが分かった.

\begin{table}[htbp]
\caption{エッジデバイス上での推論性能}
\label{table:edge_performance}
\begin{center}
\begin{tabular}{lcccc} \hline
\noalign{\vskip 3pt}
\shortstack{モデル} & \shortstack{平均レイテンシ\\(ms)} & \shortstack{スループット\\(FPS)} & \shortstack{CPU使用率\\(\%)} & \shortstack{メモリ使用量\\(MB)} \\ \hline
提案モデル & 9.05 & 110.5 & 96.5 & 3.16 \\
ResNet-50 (教師) & 364.7 & 2.7 & 97.0 & 98.5 \\
Swin-Small (教師) & 226.6 & 4.7 & 98.2 & 185.3 \\\hline
\end{tabular}
\end{center}
\end{table}